[package]
name = "num_integer_wrap"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
num-integer = { version = "0.1", default-features = false }

[profile.release]
opt-level = "s"
lto = true
